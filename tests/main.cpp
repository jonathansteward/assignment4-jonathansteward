/**
 *  Test driver for ArrayList and LinkedList. This driver aims to exercise all of the
 *  functionality of these containers in a clear and consistent way so that
 *  the class's interface can be easily inferred.
 */

#include <gtest/gtest.h>

int main(int argc, char** argv)
{
    ::testing::InitGoogleTest(&argc, argv);
    return RUN_ALL_TESTS();
}
